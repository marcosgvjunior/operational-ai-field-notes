# data/

Tiny demo inputs and optional outputs.

- `input/images/` includes `image1.png` and `image2.png` used by the notebooks.
- `output/` is optional and ignored by git by default.

Keep it small. This repo is not a dataset dump.
